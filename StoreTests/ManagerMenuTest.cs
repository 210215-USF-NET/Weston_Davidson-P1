using System;
using Xunit;

namespace StoreTests
{
    public class ManagerMenuTest
    {
        //testing data that takes no method arguments - a fact
        //methods that take in no parameters, but have an output
        // arrange, act, assert

        //Arrange stuff like object instantiation, data to pass in, etc. required for the method
        /*
        [Fact]
        public void Test1()
        {

            //act - set values as needed

            //assert - assert what should happen in a given situation

            //Assert.Equal(something, something) - assert two things are equal
        }
        */

    }
}
